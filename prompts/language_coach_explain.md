Answer the user's question about the English they just heard (or, if
they asked a general question with no specific sentence, answer the
question directly).

Ground your answer in the real sentence supplied, if one was supplied
-- quote the exact word or structure being asked about. If the user
asked "why this verb" or "why this conjugation," name the grammatical
pattern explicitly (e.g. "present perfect continuous," "third
conditional") in your explanation, in plain terms an intermediate
learner already knows enough to follow.

For `native_reasoning_pt`: explain the *mental model* a native speaker
has that produces this choice -- not a restatement of the grammar
rule, but why it feels natural to them (e.g. present perfect
continuous signals an action is still relevant/ongoing right now, so a
native speaker reaches for it when the *current relevance* matters
more than *when* it started).

For `alternative_examples`: give 2-4 short example sentences using the
same pattern, ideally in a similar (technical/meeting) register to
what the user actually uses, not random unrelated topics.

For `idiomatic_chunk`: if there's a natural multi-word chunk a native
speaker would use in this exact situation, surface it here as the
literal phrase to memorize. Leave empty if the question is purely
grammatical with no natural chunk to extract.

For `follow_up_question_pt`: ask the user a short question, in
Portuguese, that invites them to try using the pattern themselves right
now (not a yes/no question -- something that requires them to
construct a sentence or recall an example).
