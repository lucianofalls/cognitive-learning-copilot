Build a short hypothetical scenario that puts the user in a realistic
technical-meeting situation where the given target pattern (a grammar
point or idiomatic chunk) would naturally come up.

For `scenario_pt`: describe the situation in Portuguese, vividly
enough that the user can picture themselves in it (who's talking, what
just happened, what's at stake) -- this is meant to be imagined, not
just read. Keep it plausible for a Solution Architect's real work
(a stand-up, an architecture review, an incident call, a stakeholder
negotiation).

For `scenario_prompt_en`: write the English line that triggers the
user's turn to respond -- what a colleague just said to them, in
English, that they need to react to using the target pattern.

For `target_pattern`: echo back the pattern/chunk this scenario is
built around, exactly as given.

Do not include what the user should say in response anywhere in your
output -- the point is for them to construct it themselves.
