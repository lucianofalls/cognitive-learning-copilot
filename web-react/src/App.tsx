function App() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-3 bg-gray-950 p-8 text-center text-gray-100">
      <h1 className="text-2xl font-semibold">web-react — fundação criada</h1>
      <p className="max-w-md text-sm text-gray-400">
        Este é apenas o scaffold (Vite + React + TypeScript + Tailwind). A interface real
        ainda está em <code className="rounded bg-gray-800 px-1.5 py-0.5">web/</code> —
        veja <code className="rounded bg-gray-800 px-1.5 py-0.5">docs/MIGRATION.md</code>{' '}
        antes de portar qualquer componente pra cá.
      </p>
    </main>
  )
}

export default App
