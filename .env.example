# This app does not require any API keys -- it only talks to loopback
# services (whisper-stream subprocess, Ollama at 127.0.0.1:11434).
# This file documents the one supported environment variable.

# Set to "true" only for local development if you need to see transcript
# or prompt content in logs. Never enable this during a real meeting.
DEBUG_CONTENT=false
